generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                String              @id @default(uuid())
  email             String?             @unique
  name              String?
  mobile            String?             @unique
  hashedPassword    String?
  provider          Provider?
  Image             String?
  providerAccountId String?
  isMobileVerified  Boolean             @default(false)
  Balance           Balance?
  OnRampTransaction OnRampTransaction[]
  toTranactions     p2ptransactions[]   @relation("toTranactions")
  sentTranactions   p2ptransactions[]   @relation("sentTranactions")
  Received_Requets Requests[] @relation("Received_Requets")
  Sent_Requests    Requests[] @relation("Sent_Requests")
}

model p2ptransactions {
  id         Int       @id @default(autoincrement())
  tTime      DateTime? @default(now())
  amount     Int
  receiverId String
  senderId   String
  recMobile  String?
  sendMobile String?
  to         User      @relation("toTranactions", fields: [receiverId], references: [id])
  from       User      @relation("sentTranactions", fields: [senderId], references: [id])
}

model OnRampTransaction {
  id        Int          @id @default(autoincrement())
  status    OnRampStatus
  token     String       @unique
  provider  String
  amount    Int
  startTime DateTime
  userId    String
  user      User         @relation(fields: [userId], references: [id])
}

model Balance {
  id     Int    @id @default(autoincrement())
  userId String @unique
  amount Int
  locked Int
  user   User   @relation(fields: [userId], references: [id])
}

model Requests {
  id Int @id @default(autoincrement())
  fromNum String
  toNum String
  fromId String
  toId String
  fromMsg String?
  toMsg String?
  amount Int
  status OnRampStatus
  user1 User @relation("Received_Requets",fields: [fromId],references: [id]) 
  user2 User @relation("Sent_Requests",fields: [toId] , references: [id])
}

enum OnRampStatus {
  Success
  Failure
  Processing
}

enum Provider {
  local
  google
  github
}

